{
  "name": "duel",
  "version": "1.0.0",
  "private": true,
  "description": "결투 — 신호가 나오면 먼저 쏘는 사람이 이기는 반응속도 게임",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node test/rules.js",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=20"
  }
}
